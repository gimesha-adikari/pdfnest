"use client";
import { EditorLanguageChoice } from "./model";
export function EditorLanguageControl({ value, onChange, disabled = false }: { value: EditorLanguageChoice; onChange: (value: EditorLanguageChoice) => void; disabled?: boolean }) { const selected = value.mode === "AUTO" ? "auto" : value.languages[0] ?? "eng"; return <label className="flex items-center gap-2 text-xs font-medium">OCR language<select aria-label="OCR language" value={selected} disabled={disabled} onChange={(event) => { const next = event.target.value; onChange(next === "auto" ? { mode: "AUTO", languages: ["eng", "sin", "tam"] } : { mode: "EXPLICIT", languages: [next as "eng" | "sin" | "tam"] }); }} className="rounded border border-current/20 bg-transparent px-2 py-1.5"><option value="auto">Auto (English, Sinhala, Tamil)</option><option value="eng">English</option><option value="sin">Sinhala</option><option value="tam">Tamil</option></select></label>; }
