import StudioSessions from "@/components/dashboard/StudioSessions";

export default function StudioSessionsPage() {
  return <main className="min-h-screen bg-[var(--background)] text-[var(--foreground)]"><StudioSessions /></main>;
}
